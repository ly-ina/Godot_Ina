# Test Push

This file is used to test if push still works after token deletion.

If you see this file on GitHub, it means the old token is still valid (not fully revoked yet).

If the push fails with 401 Unauthorized, it means the token has been properly revoked.
