# 2026-07-01 工作日志

## godot-mcp-server 项目开发

### 完成的工作
- 初始化 TypeScript + MCP SDK 项目脚手架
- 实现 `list_scenes` Tool（扫描项目目录，返回 .tscn 文件列表）
- 实现 `read_scene` Tool（读取并解析 .tscn 场景文件）
- 实现 `.tscn` 解析器基础框架（src/parsers/tscn-parser.ts）
- 修复解析器 bug：正确解析 `parent="."` 的节点（之前只解析出1个节点，修复后能解析全部5个节点）
- 推送到 GitHub：https://github.com/ly-ina/Godot_Ina
- 配置 SSH 认证（解决 SSL 连接错误）

### 调试经验
- **写调试日志是好习惯**：在开发解析器时，用 `console.error()` 输出中间状态，快速定位了 `parent="."` 处理错误的问题
- 日志写在 `stderr` 不会影响 MCP 的 `stdout` JSON-RPC 通信
- 调试完后清理日志，但复杂逻辑保留关键日志有助于后续维护

### 技术决策
- 使用 SSH 而非 HTTPS 推送代码（避免 Windows SSL 错误）
- .tscn 解析器优先支持 Godot 4.x（format=3）
- 解析器保留 `parent="."` 原值，在 buildNodeTree 中统一解析

